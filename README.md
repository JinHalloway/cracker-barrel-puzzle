# cracker-barrel-puzzle

Before running program, make sure to install the "lambda" package to SWI Prolog.
This can be done by typing "pack_install(lambda)." to the query line after staring SWI Prolog.
